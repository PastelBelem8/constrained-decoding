# Use this file to provide the default configurations for the script "run_properties.py"
base_dir: "/extra/ucinlp1/cbelem/experiment-ro-prompts/generations/uncond"
# Use this directory to put the sampled sequences w/ properties
output_dir: "/extra/ucinlp1/cbelem/experiment-ro-prompts/generations-results/uncond-130k"
# Model we're analysing the sampled for
model_name: "EleutherAI/pythia-70m"
# We will sample from each decoding file that we specify
decoding: multinomial
# Sample 20_000 sequence
num_samples: 100_000
# Sampling seed
seed: 98123
# default batch_size
batch_size: 32